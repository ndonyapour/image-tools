"""DICOM to NIFTI."""

from pathlib import Path


def dicom2nifti(inp_dir: Path, filepattern: str, out_dir: Path):
    """DICOM to NIFTI.

    Args:
        inp_dir: input directory to process
        filepattern: filepattern to filter inputs
        out_dir: output directory
    Returns:
        None
    """
    pass